<?php require('template/header.phtml'); ?>

<div class="container mx-auto p-4">
    <h1 class="text-2xl font-bold mb-4">My Charge Points</h1>

    <div id="chargePointsContainer">
        <?php if (empty($chargePoints)): ?>
            <div class="bg-gray-100 p-4 rounded text-center">
                <p class="mb-4">You haven't added any charge points yet.</p>
                <button id="addChargePointBtn" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                    Add Charge Point
                </button>
            </div>
        <?php else: ?>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                <?php foreach ($chargePoints as $point): ?>
                    <div class="bg-white shadow-md rounded-lg p-4 relative">
                        <img src="<?= htmlspecialchars($point['charge_point_picture_url']) ?>" 
                             alt="Charge Point" 
                             class="w-full h-48 object-cover rounded mb-4">
                        
                        <div class="mb-2">
                            <strong>Address:</strong> 
                            <?= htmlspecialchars($point['streetName']) ?>, 
                            <?= htmlspecialchars($point['city_name']) ?>
                        </div>
                        
                        <div class="mb-2">
                            <strong>Price per kWh:</strong> 
                            <?= htmlspecialchars($point['price_per_kwh']) ?>
                        </div>
                        
                        <div class="flex justify-between mt-4">
                            <button class="edit-charge-point bg-yellow-500 text-white px-3 py-1 rounded" 
                                    data-charge-point-id="<?= $point['charge_point_id'] ?>">
                                Edit
                            </button>
                            <button class="delete-charge-point bg-red-500 text-white px-3 py-1 rounded" 
                                    data-charge-point-id="<?= $point['charge_point_id'] ?>">
                                Delete
                            </button>
                        </div>
                    </div>
                <?php endforeach; ?>
                
                <div class="col-span-full text-center mt-4">
                    <button id="addChargePointBtn" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                        Add Another Charge Point
                    </button>
                </div>
            </div>
        <?php endif; ?>
    </div>

    <!-- Charge Point Modal -->
    <div id="chargePointModal" class="fixed inset-0 bg-black bg-opacity-50 z-50 hidden flex items-center justify-center">
        <div class="bg-white p-6 rounded-lg w-full max-w-2xl max-h-[90vh] overflow-y-auto">
            <h2 id="modalTitle" class="text-xl font-bold mb-4">Add Charge Point</h2>
            
            <form id="chargePointForm" enctype="multipart/form-data">
                <input type="hidden" id="chargePointId" name="charge_point_id">
                <input type="hidden" id="existing_picture_url" name="existing_picture_url">

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block mb-2">Street Name</label>
                        <input type="text" name="streetName" id="streetName" 
                               class="w-full border rounded p-2" required>
                        <span class="text-red-500 error-message" id="streetName-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">City</label>
                        <select name="city_id" id="city_id" class="w-full border rounded p-2" required>
                            <option value="">Select City</option>
                            <?php foreach ($cities as $city): ?>
                                <option value="<?= $city['city_id'] ?>">
                                    <?= htmlspecialchars($city['city_name']) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                        <span class="text-red-500 error-message" id="city_id-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">Postcode</label>
                        <input type="text" name="postcode" id="postcode" 
                               class="w-full border rounded p-2" required>
                        <span class="text-red-500 error-message" id="postcode-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">House Number</label>
                        <input type="number" name="house_number" id="house_number" 
                               class="w-full border rounded p-2" required>
                        <span class="text-red-500 error-message" id="house_number-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">Road</label>
                        <input type="number" name="road" id="road" 
                               class="w-full border rounded p-2" required>
                        <span class="text-red-500 error-message" id="road-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">Block</label>
                        <input type="number" name="block" id="block" 
                               class="w-full border rounded p-2" required>
                        <span class="text-red-500 error-message" id="block-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">Price per kWh</label>
                        <input type="number" step="0.01" name="price_per_kwh" id="price_per_kwh" 
                               class="w-full border rounded p-2" required>
                        <span class="text-red-500 error-message" id="price_per_kwh-error"></span>
                    </div>

                    <div>
                        <label class="block mb-2">Charge Point Picture</label>
                        <input type="file" name="charge_point_picture" id="charge_point_picture" 
                               accept="image/jpeg,image/png,image/gif" 
                               class="w-full border rounded p-2">
                        <span class="text-red-500 error-message" id="charge_point_picture-error"></span>
                    </div>
                </div>

                <div class="mt-4">
                    <label class="block mb-2">Location</label>
                    <div id="mapContainer" class="w-full h-64"></div>
                    <input type="hidden" name="latitude" id="latitude" required>
                    <input type="hidden" name="longitude" id="longitude" required>
                    <span class="text-red-500 error-message" id="location-error"></span>
                </div>

                <div class="flex justify-end mt-4">
                    <button type="button" id="closeModalBtn" class="mr-4 bg-gray-300 px-4 py-2 rounded">
                        Cancel
                    </button>
                    <button type="submit" id="saveChargePointBtn" class="bg-blue-500 text-white px-4 py-2 rounded">
                        Save Charge Point
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- External Scripts -->
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAjJVMZgeuOeEypf2xCiIs7vIOxXH4pakw&libraries=places"></script>
<script src="js/chargePoint1.js"></script>

<?php require('template/footer.phtml'); ?>