<?php require('template/header.phtml') ?>

<style>

        .card {
            margin-bottom: 20px;
            border: none;
            background-color: #e9ecef;
            padding: 20px;
            border-radius: 5px;
        }

        .calendar {
            margin-top: 20px;
        }

        .calendar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 10px;
        }

        .day {
            padding: 10px;
            text-align: center;
            border: 1px solid #dee2e6;
            border-radius: 5px;
            background-color: #ffffff;
            cursor: pointer; /* Change cursor to pointer for clickable days */
            transition: background-color 0.3s;
        }

        .day:hover {
            background-color: #e9ecef; /* Highlight on hover */
        }

    </style>

<div class="container mt-5">
    <h1>Welcome Back!</h1>

    <div class="row">
        <!-- Active Reservations -->
        <div class="col-md-6">
            <div class="card">
                <h5>Active Reservations</h5>
                <p><?= htmlspecialchars($view->activeReservation); ?></p>
            </div>
        </div>
        <!-- Total Borrowings -->
        <div class="col-md-6">
            <div class="card">
                <h5>Total Borrowings</h5>
                <p><?= htmlspecialchars($view->borrowingCount); ?></p>
            </div>
        </div>
    </div>

    <!-- Calendar Section -->
    <div class="calendar">
        <h4>My Calendar</h4>
        <div class="calendar-header">
            <button id="prevMonth" class="btn btn-secondary">Previous</button>
            <h5 id="monthYear"></h5>
            <button id="nextMonth" class="btn btn-secondary">Next</button>
        </div>
        <div class="calendar-grid" id="calendar"></div>
    </div>
</div>

    <br><br><br><br>
<script src="js/homeRentalUser.js"></script>
<?php require('template/footer.phtml') ?>
