<!DOCTYPE html>
<html lang="en">
<head>
    <title>Forgot Password</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/font-awesome.min.css">
    <link rel="stylesheet" href="css/forgotPassword.css">
    
</head>
<body>
<div class="container">
    <div class="row auth-main-card">
        <div class="col-md-6 p-0 auth-image-column">
            <img src="images/login/login-img.jpg" alt="Forgot Password background" class="auth-image">
            <div class="auth-image-text-overlay">
                <h2>Reset Access</h2>
                <p>Enter your email to reset your password</p>
            </div>
        </div>
        <div class="col-md-6 auth-form-column">
            <div class="login-form-content">
                <h1 class="auth-page-heading">Forgot Password</h1>
                <?php if (!empty($view->message)) : ?>
                    <div class="alert alert-info"><?= htmlspecialchars($view->message) ?></div>
                <?php endif; ?>
                <?php if (!empty($view->errorMessage)) : ?>
                    <div class="alert alert-danger"><?= htmlspecialchars($view->errorMessage) ?></div>
                <?php endif; ?>
                <form method="POST">
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" name="email" id="email" class="auth-input" required value="<?= $view->email ?? '' ?>" placeholder="Enter your email">
                    </div>
                    <div class="login-button-container">
                        <button type="submit" name="send_token" class="auth-submit-button">Send Token</button>
                    </div>
                    <?php if ($view->tokenSent ?? false): ?>
                        <div class="form-group">
                            <label for="token">Token</label>
                            <input type="text" name="token" id="token" class="auth-input" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" required placeholder="Enter 6-digit token">
                            <div id="token-error-message" class="error-message" style="display: none;"></div>
                        </div>
                        <div class="form-group">
                            <label for="new_password">New Password</label>
                            <input type="password" name="new_password" id="new_password" class="auth-input" required placeholder="Create a strong password">
                            <div class="password-requirements">
                                <div id="length-req" class="requirement-not-met">• At least 8 characters</div>
                                <div id="uppercase-req" class="requirement-not-met">• At least 1 uppercase letter</div>
                                <div id="lowercase-req" class="requirement-not-met">• At least 1 lowercase letter</div>
                                <div id="number-req" class="requirement-not-met">• At least 1 number</div>
                                <div id="special-req" class="requirement-not-met">• At least 1 special character (!@#$%^&*)</div>
                            </div>
                        </div>
                        <div class="login-button-container">
                            <button type="submit" name="reset_password" class="auth-submit-button">Reset Password</button>
                        </div>
                    <?php endif; ?>
                </form>
                <p class="auth-link-text"><a href="login.php" class="auth-link">Back to Login</a></p>
            </div>
        </div>
    </div>
</div>

<script src="js/reset_password.js"></script>
</body>
</html>