<!DOCTYPE html>
<html lang="en">
<head>
    <title><?= htmlspecialchars($view->pageTitle) ?></title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <link rel="stylesheet" href="css/bootstrap.min.css">
    <link rel="stylesheet" href="css/font-awesome.min.css">
    <link rel="stylesheet" href="css/owl.carousel.css">
    <link rel="stylesheet" href="css/owl.theme.default.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>

<body class="nopadding">
    <div class="login-container">
        <div class="login-box">
            <h1>Login</h1>
            <p>Don't have an account? <a href="register.php">Register</a></p>

            <?php if (!empty($view->errorMessage)) : ?>
                <div class="alert alert-danger"><?= htmlspecialchars($view->errorMessage) ?></div>
            <?php endif; ?>

            <form action="" method="POST" id="login-form">
                <div class="input-grouplogin">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required value="<?= $view->email ?>">
                    <div class="error-message" id="email-error"></div> <!-- Email error message -->
                </div>
                <div class="input-grouplogin">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required value="<?= isset($_POST['password']) ? htmlspecialchars($_POST['password']) : '' ?>">
                    <div class="error-message" id="password-error"></div> <!-- Password error message -->
                </div>
                <button type="submit" class="login-btn">Login</button>
            </form>
        </div>
    </div>
</body>
</html>