<?php require('template/header.phtml'); ?>
<link rel="stylesheet" href="css/charge-point-management.css">

<h1><?php echo htmlspecialchars($view->pageTitle); ?></h1>
<div id="message-container">
    <?php if ($view->message): ?>
        <div class="message <?php echo $view->messageType; ?>"><?php echo htmlspecialchars($view->message); ?></div>
    <?php endif; ?>
</div>
<button id="addChargePointBtn" class="btn btn-primary mb-3">
    Add Charge Point
</button>

<!-- Homeowner Selection Modal -->
<div class="modal fade" id="homeownerModal" tabindex="-1" aria-labelledby="homeownerModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="homeownerModalLabel">Select Homeowner</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <?php if (!empty($view->homeownersWithoutChargePoints)): ?>
                    <ul class="list-group">
                        <?php foreach ($view->homeownersWithoutChargePoints as $user): ?>
                            <li class="list-group-item list-group-item-action homeowner-item" data-user-id="<?php echo htmlspecialchars($user['user_id']); ?>">
                                <?php echo htmlspecialchars($user['first_name'] . ' ' . $user['last_name'] . ' (ID: ' . $user['user_id'] . ')'); ?>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php else: ?>
                    <div class="alert alert-info">
                        All homeowners currently have charge points assigned. No homeowners available.
                    </div>
                <?php endif; ?>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<table class="data-table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Owner</th>
            <th>Price per kWh</th>
            <th>Availability</th>
            <th>Address</th>
            <th>Picture</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody id="charge-points-container">
        <?php foreach ($view->chargePoints as $chargePoint): ?>
            <tr data-id="<?php echo htmlspecialchars($chargePoint['charge_point_id']); ?>">
                <td><?php echo htmlspecialchars($chargePoint['charge_point_id']); ?></td>
                <td><?php echo htmlspecialchars($chargePoint['owner']); ?></td>
                <td><?php echo htmlspecialchars($chargePoint['price_per_kwh']); ?></td>
                <td><?php echo htmlspecialchars($chargePoint['availability']); ?></td>
                <td><?php echo htmlspecialchars($chargePoint['streetName']); ?></td>
                <td><img src="<?php echo htmlspecialchars($chargePoint['charge_point_picture_url']); ?>" alt="Charge Point" class="thumbnail"></td>
                <td class="actions">
                    <button class="btn btn-edit" onclick="window.location.href='adminEditChargePoint.php?id=<?php echo htmlspecialchars($chargePoint['charge_point_id']); ?>'">Edit</button>
                    <button class="btn btn-delete" onclick="deleteChargePoint(<?php echo htmlspecialchars($chargePoint['charge_point_id']); ?>, this)">Delete</button>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>

<script>
    // Add Charge Point Modal Functionality
    document.addEventListener('DOMContentLoaded', function() {
        // Get the button that opens the modal
        const addChargePointBtn = document.getElementById('addChargePointBtn');
        
        // Get the modal
        const homeownerModal = new bootstrap.Modal(document.getElementById('homeownerModal'));
        
        // When the user clicks on the button, open the modal
        addChargePointBtn.addEventListener('click', function() {
            homeownerModal.show();
        });
        
        // Add click event listeners to all homeowner items
        const homeownerItems = document.querySelectorAll('.homeowner-item');
        homeownerItems.forEach(function(item) {
            item.addEventListener('click', function() {
                const userId = this.getAttribute('data-user-id');
                window.location.href = 'adminAddChargePoint.php?user_id=' + userId;
            });
        });
    });
</script>
<script src="js/adminManageChargePoint.js"></script>
<?php require('template/footer.phtml'); ?>